# Fabric System Test Configuration
# Copy this file to .env and fill in your values

# Microsoft Fabric Configuration (REQUIRED)
FABRIC_WORKSPACE_ID=your-workspace-id-here
FABRIC_LAKEHOUSE_ID=your-lakehouse-id-here

# Azure Authentication (OPTIONAL - if not set, will use 'az login' credentials)
# Only needed for service principal authentication (CI/CD)
# For local development, just run 'az login' instead
# AZURE_TENANT_ID=your-tenant-id-here
# AZURE_CLIENT_ID=your-client-id-here
# AZURE_CLIENT_SECRET=your-client-secret-here

# Optional: Databricks Configuration (for Databricks tests)
# DATABRICKS_HOST=https://your-workspace.azuredatabricks.net
# DATABRICKS_TOKEN=your-access-token-here
# DATABRICKS_CLUSTER_ID=your-existing-cluster-id  # Optional: use existing cluster instead of creating new one

# Optional: Synapse Configuration (for Synapse tests)
# SYNAPSE_WORKSPACE_NAME=your-workspace-name-here
# SYNAPSE_SPARK_POOL_NAME=your-spark-pool-name-here
# Note: Synapse tests also require AZURE_TENANT_ID, AZURE_CLIENT_ID, AZURE_CLIENT_SECRET

# Test Configuration
# TEST_TIMEOUT=600           # Job execution timeout in seconds (default: 600)
# POLL_INTERVAL=10           # Status poll interval in seconds (default: 10)

# Notes:
# 1. Never commit the actual .env file with real credentials
# 2. Fabric workspace and lakehouse IDs can be found in Fabric portal URLs
# 3. Service principal must have Contributor role on workspace
# 4. For CI/CD, use secrets instead of .env file
# 5. Databricks cluster creation requires "Allow cluster creation" permission
#    - If you don't have this permission, create a cluster manually in Databricks UI:
#      * Go to Compute → Create Cluster
#      * Name: kindling-test-cluster
#      * Access Mode: Single User
#      * Policy: Personal Compute (or your org's policy)
#      * Runtime: 13.3.x LTS (Scala 2.12, Spark 3.4.1)
#      * Node type: Standard_DS3_v2
#      * Workers: 0 (single node)
#      * Auto-terminate: 120 minutes
#    - Copy the cluster ID from the URL and add it as DATABRICKS_CLUSTER_ID
