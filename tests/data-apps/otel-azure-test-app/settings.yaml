kindling:
  extensions:
    - kindling-otel-azure>=0.3.0a3

  bootstrap:
    databricks_volume_path: "/Volumes/medallion/default/temp"

  telemetry:
    azure_monitor:
      enable_logging: true
      enable_tracing: true
      connection_string: "${APPLICATIONINSIGHTS_CONNECTION_STRING}"
