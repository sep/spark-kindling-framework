name: complex-tracing-test
version: 0.1.0
description: Complex tracing system test with variable durations and nested spans

# No extension specified - works with both default and Azure Monitor telemetry
# To test with Azure Monitor, add:
# extensions:
#   - kindling-otel-azure>=0.1.0
#
# extension_config:
#   azure_monitor:
#     connection_string: "InstrumentationKey=..."
