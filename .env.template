# Kindling Framework Environment Configuration Template
# Copy this file to .env and fill in your specific values
# Note: .env files should NEVER be committed to version control

# Azure Configuration
# ===================

# Azure Cloud Environment (public, government, china)
AZURE_CLOUD=public

# Azure Storage Configuration
AZURE_STORAGE_ACCOUNT=your-storage-account-name
AZURE_CONTAINER=your-container-name
AZURE_BASE_PATH=kindling/tests

# Azure Storage Endpoint Suffix (varies by cloud)
# public: blob.core.windows.net / dfs.core.windows.net
# government: blob.core.usgovcloudapi.net / dfs.core.usgovcloudapi.net
# china: blob.core.chinacloudapi.cn / dfs.core.chinacloudapi.cn
# Azure Authentication (choose one method)
# =========================================

# Method 1: Storage Account Key (simple, for development)
AZURE_STORAGE_KEY=your-storage-account-key

# Method 2: Service Principal / OAuth (recommended for production)
AZURE_TENANT_ID=your-tenant-id
AZURE_CLIENT_ID=your-client-id
AZURE_CLIENT_SECRET=your-client-secret

# Method 3: Azure CLI (no additional vars needed, just run 'az login')
# Azure CLI Client ID (usually the well-known Azure CLI client ID)
AZURE_CLI_CLIENT_ID=04b07795-8ddb-461a-bbee-02f9e1bf7b46

# Microsoft Fabric Configuration
# ==============================

# Fabric Workspace ID (only needed for Fabric environments)
FABRIC_WORKSPACE_ID=your-fabric-workspace-id

# Fabric Lakehouse ID (only needed for Fabric environments)
FABRIC_LAKEHOUSE_ID=your-fabric-lakehouse-id

# Databricks Configuration
# ========================

# Databricks Workspace URL
DATABRICKS_WORKSPACE_URL=https://your-workspace.cloud.databricks.com

# Databricks Token (for authentication)
DATABRICKS_TOKEN=your-databricks-token

# Synapse Configuration
# ====================

# Synapse Workspace Name
SYNAPSE_WORKSPACE_NAME=your-synapse-workspace

# Synapse SQL Pool Name (if using SQL pools)
SYNAPSE_SQL_POOL_NAME=your-sql-pool

# Testing Configuration
# =====================

# Test Storage Account (for integration tests)
TEST_STORAGE_ACCOUNT=your-test-storage-account
TEST_CONTAINER=test-data

# Test Workspace IDs (for platform-specific tests)
TEST_FABRIC_WORKSPACE_ID=your-test-fabric-workspace-id
TEST_DATABRICKS_WORKSPACE_URL=https://your-test-workspace.cloud.databricks.com
