# Kindling Framework Environment Configuration Template
# Copy this file to .env and fill in your specific values
# Note: .env files should NEVER be committed to version control

# Azure Configuration
# ===================

# Azure Cloud Environment (AzurePublicCloud, AzureUSGovernment, AzureChinaCloud)
export AZURE_CLOUD="AzurePublicCloud"
# Optional storage endpoint overrides. Kindling derives endpoints from
# AZURE_CLOUD by default; use these only to force a custom endpoint.
# export AZURE_STORAGE_DFS_ENDPOINT_SUFFIX=
# export AZURE_STORAGE_BLOB_ENDPOINT_SUFFIX=
# Azure Storage Configuration

export AZURE_SUBSCRIPTION_ID=
export AZURE_RESOURCE_GROUP=

export AZURE_STORAGE_ACCOUNT=
export AZURE_CONTAINER=
export AZURE_BASE_PATH=

# Fabric Config (only needed for Fabric workspaces)
export FABRIC_WORKSPACE_ID=""
export FABRIC_LAKEHOUSE_ID=""

# Synapse Config (only needed for Synapse workspaces)
export SYNAPSE_WORKSPACE_NAME=
export SYNAPSE_SPARK_POOL=

# Databricks Config (only needed for Databricks workspaces)
export DATABRICKS_HOST=
export DATABRICKS_CLUSTER_ID=

# Optional: Secret provider config overrides for system tests (kindling.secrets.*)
# Use either linked service names or key vault URLs
# export CONFIG__platform_fabric__kindling__secrets__linked_service=
# export CONFIG__platform_synapse__kindling__secrets__linked_service=
# export CONFIG__platform_fabric__kindling__secrets__key_vault_url=
# export CONFIG__platform_synapse__kindling__secrets__key_vault_url=
# export SYSTEM_TEST_KEY_VAULT_URL=
# Optional databricks override (default scope is kindling-system-tests)
# export CONFIG__platform_databricks__kindling__secrets__secret_scope=

export AZURE_CLIENT_SECRET=""
export AZURE_CLIENT_ID=""
export AZURE_TENANT_ID=""
