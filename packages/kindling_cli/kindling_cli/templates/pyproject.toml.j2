[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "{{ kebab_name }}"
version = "0.1.0"
description = "Kindling data pipeline project — {{ kebab_name }}"
authors = []
packages = [{include = "{{ snake_name }}", from = "src"}]

[tool.poetry.dependencies]
python = "^3.10"
# Kindling wheels are baked into the devcontainer image at /opt/kindling-packages/wheels/.
# Poetry resolves them directly from disk — no PyPI connection required.
spark-kindling = {path = "/opt/kindling-packages/wheels/spark_kindling-{{ kindling_version }}-py3-none-any.whl", extras = ["standalone"]}

[tool.poetry.group.dev.dependencies]
pytest = ">=7.0.0"
pytest-cov = ">=4.0.0"
poethepoet = ">=0.24.0"
spark-kindling-cli = {path = "/opt/kindling-packages/wheels/spark_kindling_cli-{{ kindling_version }}-py3-none-any.whl"}

[tool.poe.tasks]
test-unit = "pytest tests/unit -v"
test-component = "pytest tests/component -v"
{% if integration %}
test-integration = "pytest tests/integration -v"
test-all = { sequence = ["test-unit", "test-component", "test-integration"] }
{% endif %}
test = { sequence = ["test-unit", "test-component"] }
build = "poetry build"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
markers = [
    "unit: local Spark unit tests, no ABFSS",
    "component: DI wiring, no ABFSS",
{% if integration %}
    "integration: requires Spark and ABFSS credentials",
    "requires_azure: skipped when Azure credentials are not set",
{% endif %}
]
