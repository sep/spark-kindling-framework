FROM ghcr.io/sep/spark-kindling-framework/devcontainer:{{ kindling_version }}
# Everything you need is already in the base image:
#   Python 3.11, Java 21, Poetry, spark-kindling wheels, Hadoop Azure JARs
#
# Add project-specific system deps below if needed, e.g.:
#   RUN apt-get update && apt-get install -y --no-install-recommends <pkg>
