"""{{ kebab_name }} — Kindling file-ingestion app entrypoint.

Run locally:    kindling app run .
Run remotely:   kindling app run . --platform <platform>

Set KINDLING_INGESTION_PATH to the source path to ingest. Entities are
declared in the {{ package_kebab_name }} package (see lake-reqs.txt) and
registered automatically -- no imports needed here.
"""

if __name__ == "__main__":
    from kindling.apps import run_file_ingestion_app

    run_file_ingestion_app()
