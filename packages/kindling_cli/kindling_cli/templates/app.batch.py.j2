"""{{ kebab_name }} — Kindling batch app entrypoint.

Run locally:    kindling app run .
Run remotely:   kindling app run . --platform <platform>

Entities and pipes are declared in the {{ package_kebab_name }} package
(see lake-reqs.txt) and registered automatically -- no imports needed here.
"""

if __name__ == "__main__":
    from kindling.apps import run_batch_app

    run_batch_app()
